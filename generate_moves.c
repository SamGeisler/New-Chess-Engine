#include "global.h"
#include "generate_moves.h"
#include "control.h"

uint64_t gen_pawn(int src);
uint64_t gen_knight(int src);
uint64_t gen_bishop(int src);
uint64_t gen_rook(int src);
uint64_t gen_king(int src);

//Move array index
int m_i;

int generate_moves(move* move_arr, int color){
    m_i = 0;



    return m_i+1;
}

int is_in_check(int color){

}